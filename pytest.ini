[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
addopts = -m "not integration"

markers =
    unit: fast isolated unit tests
    integration: tests that touch I/O or external services (requires ANTHROPIC_API_KEY)
